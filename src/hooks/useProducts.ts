/**
 * useProducts — Clean re-export (replaces useRealProducts)
 * Delegates to useProductsUnified
 */
export { useRealProducts as useProducts } from './useRealProducts'
export type { Product } from './useRealProducts'
