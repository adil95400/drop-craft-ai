/**
 * useWinners — Clean re-export (replaces useRealWinners)
 */
export { useRealWinners as useWinners } from './useRealWinners'
export type { WinningProduct, WinnersStats } from './useRealWinners'
